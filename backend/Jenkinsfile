pipeline {
   environment {
     FOO = "foo"
   }

   agent any
   stages {
       stage("first") {
           steps {
               sh "echo ${FOO}"
               sh "ls -a"
               sh "pwd"
           }
       }
   }
}